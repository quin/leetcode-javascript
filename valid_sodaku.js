function validSodaku(data) {
  rows = data;
  cols = [];
  grid = [];

  for (var i = 0; i < 9; i++) {
    cols.push([]);
    grid.push([]);
  }

  for (var j = 0; j < 9; j++) {
    for (var k = 0; k < 9; k++) {
      
    }
  }
}
